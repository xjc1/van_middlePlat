import { Meta, Preview, Story, Props } from '@storybook/addon-docs/blocks';
import { Form, Button } from 'antd';
import { ScenceMultiTable } from '@/components/tis_ui';

<Meta
  title="表单组件/MultiTable/ScenceMultiTable"
  component={ScenceMultiTable}
  decorators={[storyFn => <div text="ffff" style={{ padding: 20, minHeight: 300 }}>{storyFn()}</div>]}
/>

# ScenceMultiTable `带表格的添加主题的自定义控件`

<Preview>
  <Story name="index">
    <Form>
      <Form.Item name="matter" label="带表格的主题选择框">
        <ScenceMultiTable showHeader />
      </Form.Item>
    </Form>
  </Story>
</Preview>

### 参数

<Props of={ScenceMultiTable} />
