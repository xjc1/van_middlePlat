/**
 * iframe 页面路径
 * 使用方式：
 *      在配置路由表时使用 iframe 路由常量。
 * 例如：
 *      {
 *        ...
 *        path: '/content/archivesLibMenu/archivesLib'
 *        target: iframePagesEnum.ARCHIVESLIB,
 *        component: './IframePages',
 *        ...
 *      }
 */
module.exports = {
  // 档案库路由
  ARCHIVESLIB: '/arch/arch',
}